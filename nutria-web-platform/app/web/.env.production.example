# Optional: public API origin for static builds (no trailing slash).
# If unset, the app uses same-origin /api/... (works with `vite` dev + proxy, not on github.io alone).
# VITE_API_BASE=https://your-deployed-hono-app.example.com
